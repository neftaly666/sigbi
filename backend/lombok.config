config.stopBubbling = true

# Sin esto @Qualifier se queda en el campo y no llega al parametro del constructor
# que genera @RequiredArgsConstructor, con lo que la inyeccion solo acertaria por nombre.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
